import "chai/register-assert";
import * as winstonUtils from "@atypon/winston-utils";
import winston from "winston";

describe("Test new library", async function() {
    describe("Tests", async function() {
        it("should sanity check tests",async function() {
            assert.isOk(true);
        });
    });
});
